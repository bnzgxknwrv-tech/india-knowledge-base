# 04 — Repositorybreed master-A/B-register en KML

## Telling

Het gedeelde masterregister en de voorbereide KML zijn opnieuw volledig gelezen:

- 28 formele A;
- 2 formele B;
- 30 placemarks totaal;
- 0 C;
- 0 stations, routes, bases of hotels;
- 0 dubbele niet-lege LOCATION_ID's;
- 0 ontbrekende coördinaten.

## Kleur en lagen

De KML bevat exact twee folders:

- `A_FORMEEL` met stijl `A_GREEN`, kleur `ff00aa00` en groene pushpin;
- `B_FORMEEL` met stijl `B_ORANGE`, kleur `ff00a5ff` en oranje pushpin.

Geen A gebruikt de B-stijl en geen B gebruikt de A-stijl. Er zijn geen categorie-logo's of extra folders.

## Onafhankelijke broncontrole

De 7 Varanasi-A-ankers zijn in deze ZILVER-fase opnieuw inhoudelijk gecontroleerd. De Mahabodhi Tempel en Bodhiboom zijn aanvullend tegen UNESCO gecontroleerd (`ZVS-020`). Katyayani Peeth is tegen de eigen contactpagina gecontroleerd (`ZVS-021`). De bestaande Kumaon-GEO en LOCATION_ID's zijn uitsluitend heropend via de gepinde masterdata; de geparkeerde `KUMAON-ROUTE-OPTIMIZATION-001` is niet gelezen of gewijzigd.

## Uitkomst

Het gedeelde register en de voorbereide KML hoeven niet inhoudelijk te worden gewijzigd. Een bytegelijke ZILVER-kopie van beide wordt als gevalideerd faseartifact opgenomen.

Resultaat: `PASS`.

END_OF_ARTIFACT