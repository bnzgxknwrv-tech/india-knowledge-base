# INDIA3 ENTRYPOINT

This is the only active start point for new India research chats.

## Required preflight
1. Test GitHub read and write for `bnzgxknwrv-tech/india-knowledge-base`.
2. Read `pipeline/ACTIVE_SYSTEM.yaml`.
3. Read `pipeline/NEXT_ACTION.yaml`.
4. Confirm the active system is INDIA3, the route and expected state agree, and no conflicting active claim exists.
5. Read only the role contract, pinned context manifest and `required_files` named by NEXT_ACTION/context.

If GitHub read or write is unavailable, answer exactly:
`MARK: IK MIS GITHUB CONNECTOR!`

## Execution boundary
The controller manages state, manifests, transitions and recovery only. BRONS discovers. ZILVER verifies. GOUD integrates and produces required user delivery. No worker assigns Mark A/B/C. Unchosen candidates remain `DOOR_MARK_TE_BEOORDELEN`.

## Validation language
Never use checked, verified, definitive, complete or correct without naming what was examined, by which method, by which role, when and with what residual uncertainty.

## Tool acceptance
Before external or GEO work, apply `india3/CAPABILITY_CHECK.md` and `india3/protocols/TOOL_CAPABILITY_PROTOCOL.md`. Direct Google Maps visual verification may be claimed only when the place was actually opened and matched through an available tool.

## Normal context rule
No broad repository sweep. Read only:
- this entrypoint;
- ACTIVE_SYSTEM;
- NEXT_ACTION;
- the active role contract;
- the pinned context manifest;
- its required files.

## Completion
Use role-specific quality gates, atomic commit and readback. GOUD cannot pass without every required practical Mark product listed in `USER_DELIVERY_MANIFEST.yaml`.

## Recovery
On a defined blocker, write status, failure report and exact resume prompt before stopping. A clean chat resumes from the first incomplete validated step only.

END_OF_ARTIFACT
